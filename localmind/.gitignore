# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea 
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
.claude
.env

# Secrets — OAuth client JSON, tokens, API keys. Never commit.
secrets/
*.keys.json
gcp-oauth.keys.json
*-credentials.json
tokens.json

# Agent runtime state, not source: skills/improvements/dynamic tools the
# assistant writes into the open workspace at runtime.
.localmind/

# MCP scratch output (Playwright traces, page snapshots).
src-tauri/.playwright-mcp/
src-tauri/snapshot.md

# Rust build output.
src-tauri/target/