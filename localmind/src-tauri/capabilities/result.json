{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "result",
  "description": "Capability for the quick-invoke result widget window",
  "windows": ["result"],
  "permissions": [
    "core:default",
    "core:window:allow-hide",
    "core:window:allow-show",
    "core:window:allow-set-focus"
  ]
}
